

using Prueba_Git;

var Muestra = (string m) => Consola.Print( m );

Muestra( "Este programa solo tiene interfaz de consola." );
Muestra( "Hola desde Rider, ¡¡probando Git!!" );
Muestra( "Parece que funciona y es una herramienta útil." );
